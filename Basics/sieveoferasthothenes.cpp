#include <iostream>
using namespace std;
bool isPrime(int i){
    
}
int main()
{
    int n;
    cout << "Enter the number" << endl;
    int count = 0;
    for (int i = 0; i < n; i++)
    {
        if (isPrime(n))
        {
            count++;
        }
    }
    cout << "The number of prime is " << count << endl;
}